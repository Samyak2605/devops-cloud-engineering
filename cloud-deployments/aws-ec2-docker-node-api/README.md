# AWS EC2 Docker Deployment – Node API

This project demonstrates deploying a Dockerized Node.js API on an AWS EC2 instance.

### Steps
1. Launch Ubuntu EC2
2. Open port 3000 in Security Group
3. SSH into instance
4. Run `deploy.sh`

### Outcome
- API accessible via EC2 public IP
- Container health-checked
- Production-style deployment
